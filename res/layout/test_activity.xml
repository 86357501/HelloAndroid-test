<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" 
    tools:context="com.example.helloandroid.MainActivity">

    <FrameLayout
        android:id="@+id/id_content"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/tab_menu"
        android:orientation="vertical" >
    </FrameLayout>

    <RadioGroup
        android:id="@+id/tab_menu"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#3f3f3f"
        android:orientation="horizontal" >

        <RadioButton
            android:id="@+id/rbChat"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:button="@null"
            android:checked="true"
            android:drawableTop="@drawable/main_navigation_1"
            android:gravity="center_horizontal|bottom"
            android:paddingTop="5dp"
            android:text="首页"
            android:textColor="@drawable/main_navigation_color"
            android:textSize="12sp" />

        <RadioButton
            android:id="@+id/rbAddress"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/main_navigation_2"
            android:gravity="center_horizontal|bottom"
            android:paddingTop="5dp"
            android:text="晨读"
            android:textColor="@drawable/main_navigation_color"
            android:textSize="12sp" />

        <RadioButton
            android:id="@+id/rbFind"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/main_navigation_3"
            android:gravity="center_horizontal|bottom"
            android:paddingTop="5dp"
            android:text="同事电话"
            android:textColor="@drawable/main_navigation_color"
            android:textSize="12sp" />

        <RadioButton
            android:id="@+id/rbMe"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/main_navigation_4"
            android:gravity="center_horizontal|bottom"
            android:paddingTop="5dp"
            android:text="个人信息"
            android:textColor="@drawable/main_navigation_color"
            android:textSize="12sp" />
    </RadioGroup>

</RelativeLayout>